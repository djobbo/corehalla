# Database access for Drizzle and the Effect PostgreSQL client.
#
# `pnpm setup:env` writes this file with the local database URL from
# `supabase status`, so it only needs to be set by hand against a remote
# database (keep it out of git).

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
